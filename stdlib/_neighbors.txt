Def NthNeighbor(Index, Body) Chain {
	Index
	Area 1 Chain {
		dec()
		Filter zero() Chain { Body }
	}
} End

Def NthNeighbor(Index, Cond, BodyOuter) Chain {
	Index
	Write0("_INNER")
	Area 1 Chain {
		dec()
		Filter zero() Write1("_INNER")
	}
	Filter Is1("_INNER") Chain { BodyOuter }
} End